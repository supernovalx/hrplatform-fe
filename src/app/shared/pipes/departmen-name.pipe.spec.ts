import { DepartmenNamePipe } from './departmen-name.pipe';

describe('DepartmenNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmenNamePipe();
    expect(pipe).toBeTruthy();
  });
});
